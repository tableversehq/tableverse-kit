{
  "name": "splendor",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "engine",
    "client"
  ],
  "scripts": {
    "dev": "tvk dev",
    "build": "npm run build --workspace=client",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@tableverse-kit/cli": "^0.1.0-beta.1",
    "@tableverse-kit/config": "^0.1.0-beta.1",
    "typescript": "^5"
  }
}
