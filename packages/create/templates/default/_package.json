{
  "name": "{{projectName}}",
  "version": "0.0.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "engine",
    "client"
  ],
  "scripts": {
    "dev": "tvk dev",
    "build": "npm run build --workspace=client",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@tableverse-kit/cli": "{{cliVersion}}",
    "@tableverse-kit/config": "{{configVersion}}",
    "typescript": "^5"
  }
}
